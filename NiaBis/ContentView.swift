import SwiftUI
import SwiftData

struct ContentView: View {  
  var body: some View {
    NavigationStack {
      Text("ContentView")
        .navigationTitle("NaBis")
    }
  }
}

#Preview {
  ContentView()
}
